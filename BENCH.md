# BENCH — 성능 측정

vLLM 서버만 있으면 돌아간다. k8s 불필요.

```bash
# 0. 엔드포인트 점검 — 어떤 structured 모드가 실제로 강제되는지
make check

# 1. mock 데이터 생성 (seed 고정, 정답지 포함)
make mock

# 2. LLM 없이 하네스 오버헤드 측정 (비용 0)
make bench-offline

# 3. 실제 측정
cp .env.example .env   # 값 채우기
make bench
```

중단 후 다시 실행하면 끝난 조합은 건너뛴다(`results/bench.jsonl` 기준).

---

## 0단계를 건너뛰지 말 것

structured output 옵션은 **틀려도 에러가 나지 않는다.** 지원하지 않는 표기를
보내면 서버가 조용히 무시하고 평범한 생성으로 돌아간다.
그러면 스키마 강제가 전혀 없는 채로 "정상" 동작하는 것처럼 보인다.

`bench/check_endpoint.py`는 추측하지 않는다. **스키마 위반을 유도하는 프롬프트를
보내 실제로 차단되는지** 확인한다. 위반 응답이 그대로 나오면 강제가 없는 것이다.

```
모드          도달    강제    설명
------------------------------------------------------------
guided_json   OK      예      위반 지시에도 스키마 준수: verdict='no'
json_schema   실패    -       BadRequestError: unknown parameter
prompt        OK      아니오  위반 응답이 그대로 나옴

권장 설정:  LLM_STRUCTURED_MODE=guided_json
```

---

## 무엇을 재는가

측정을 두 종류로 **엄격히 나눈다.** 섞어서 보고하면 숫자가 거짓말을 한다.

### A. 정답 대비 정확도

`make_mock_data.py`가 데이터를 만들면서 정답을 함께 낸다. 값이 아니라
**구조적 성질**이 정답이다 — 유일성, 카디널리티, 시간 해상도, 함수 종속, PII 형태.

| 지표 | 무엇 | 누가 만드는가 |
|---|---|---|
| `kind` | 컬럼 분류 정확도 | 프로파일러 (결정론적) |
| `role` | 식별자 primary/reference 판정 | LLM + uniqueness probe |
| `grain` | 입도 키 집합 일치 | LLM + 복합 uniqueness probe |
| `res` | 시간 해상도 | LLM + time_component probe |
| `PII R/P` | 개인정보 재현율/정밀도 | LLM + regex probe |
| `FD` | 함수 종속 재현율 | 결정론적 (probe 전수) |

**`kind`가 1.00이 아니면 규칙 버그다.** LLM 탓이 아니다.
실제로 이 지표가 센서 컬럼 24개 오분류를 잡아냈다(고유값 비율만 보고
실수형 측정값을 식별자로 판정하던 버그).

**PII는 재현율과 정밀도를 따로 본다.** 미탐이 과탐보다 비싸기 때문이다.
과탐은 사람이 걷어내면 되지만 미탐은 아무도 모른다.

### B. 정답 없이 재는 것

| 지표 | 의미 | 읽는 법 |
|---|---|---|
| `sec` / `LLM` / `tok` | 테이블 1개당 비용 | 컬럼 수에 대한 스케일링을 본다 |
| `iter` | plan-act 왕복 횟수 | 컬럼 수에 비례하면 배치가 깨진 것 |
| `retry` | 1차 시도에서 반증·차단된 비율 | **0이면 좋은 게 아니다** (아래) |
| `cover` | 해석된 컬럼 / 전체 | 1.0 미만이면 격리된 컬럼이 있다 |
| `probeC` | 주장 중 데이터로 검증 가능했던 비율 | 낮으면 검증 안 된 주장이 많다는 뜻 |
| `refut` | 최종 반증 건수 | 0이 정상. 있으면 그 주장은 폐기됐다 |

### C. 반복 일관성

같은 입력을 N회 돌려 일치도를 본다.

> **일관성은 정확도가 아니다.** 같은 오답을 20번 내도 1.00이다.
> 반드시 A와 함께 봐야 의미가 있다.

`role`/`resol`은 최빈값 일치율, `keyword`/`summary`는 평균 pairwise Jaccard.

---

## 숫자 읽는 법

### retry가 0이면?

좋은 신호가 아닐 수 있다. 두 가지 해석이 가능하다.

- 프롬프트가 충분히 강해서 1차에 맞춘다 (좋음)
- **probe가 느슨해서 틀린 주장을 통과시킨다** (나쁨)

A의 정확도와 교차 확인해야 갈린다. `retry`는 0인데 `role` 정확도가 낮으면
후자다. probe 조건을 조여야 한다.

### probeC(검증률)가 0.3이면?

주장의 70%는 데이터로 확인할 수 없었다는 뜻이다. 대부분 자연어 `meaning`이다.
이건 **결함이 아니라 한계의 정직한 표시**다. 검증률을 억지로 올리려고
검증 안 되는 주장을 숨기면 그 순간 리포트가 거짓말이 된다.

올리는 방법은 하나뿐이다 — 주장을 검사 가능한 형태로 쪼개는 것.
"출력값을 W 단위로 기록한다"를 단위 주장(텍스트 근거)과 범위 주장
(`NUMERIC_RANGE` probe)으로 나누면 검증률이 오른다.

### desc(True/False) 차이

`column_descriptions`(데이터 사전) 유무 비교다. 기존 레포의 강건성 테스트와
같은 축이라 나란히 놓고 볼 수 있다.

설명을 줬는데 정확도가 안 오르면 **설명이 프롬프트에 실리지 않은 것**을 먼저
의심하라. 실제로 그 버그가 있었고, 두 조건 결과가 같아져서 실험 자체가
무효였다(`tests/test_legacy_contract.py::test_column_descriptions_reach_prompt`가 지킨다).

---

## 오프라인 모드의 한계

`--offline`은 LLM 없이 결정론적 스텁으로 돈다.
**품질 측정에는 쓸 수 없다.** 요약을 LLM이 만든 게 아니다.

쓸 수 있는 것:
- 하네스 오버헤드 (probe/플래너/직렬화가 얼마나 먹는지)
- 배치·격리·재시도 경로 동작 확인
- `kind`/`FD` 정확도 — 이 둘은 LLM과 무관한 결정론적 산출물이다

오프라인 결과에서 `res`(시간 해상도)와 `PII`가 낮은 건 정상이다.
스텁은 항상 안전한 값(Day, PII 없음)을 고르도록 만들었다.

---

## mock 데이터

| 데이터셋 | 무엇을 시험하는가 |
|---|---|
| `process_log` | 시간축 + 측정값 + 함수 종속(`equipment_id → line_id`) |
| `maintenance_history` | 분 단위 시각 해상도, 자유 텍스트 |
| `operator_roster` | PII 3종(이름·이메일·휴대폰) 탐지 |
| `wide_sensor` | 25컬럼 배치 효율, 비용 스케일링 |

seed 고정이라 재현된다. 사내 데이터를 반출할 수 없으므로 값이 아니라
**구조적 성질**만 같게 만들었다.

실제 테이블로 재려면 같은 형식으로 `<이름>.csv` + `<이름>_truth.json`을
만들면 된다. `truth.json`이 없으면 B와 C만 측정된다.
